// Generated by CoffeeScript 1.3.3

define(["jquery", "underscore", "backbone", "views/render_links", "views/add_links", "views/add_tags"], function($, _, Backbone, renderLinksView, addLinksView, addTagsView) {
  var AppRouter, initialize;
  AppRouter = Backbone.Router.extend({
    routes: {
      "*actions": "defaultRoute"
    },
    defaultRoute: function(actions) {
      console.debug("AppRouter:defaultRoute");
      addLinksView.render();
      return addTagsView.render();
    }
  });
  initialize = function() {
    var app_router;
    app_router = new AppRouter();
    return Backbone.history.start();
  };
  return {
    initialize: initialize
  };
});
